source "https://rubygems.org"

# Mirror GitHub Pages’ environment
gem "github-pages", group: :jekyll_plugins

# Local-only helpers for modern Ruby (not used by GitHub Pages itself)
gem "webrick", "~> 1.8"

# Ruby 3.4+ / 4.0 moved these out of the core bundle; Jekyll 3.9 expects them
gem "csv", "~> 3.3"
gem "bigdecimal", "~> 3.1"
